# Basic-Crud
A lightweight Spring Boot service implementing basic CRUD operations for Employee management using an in-memory storage approach. It utilizes a LinkedHashMap for data persistence and AtomicInteger to ensure thread-safe ID generation.
